#!/bin/sh

brew bundle --file=- <<EOF
cask "dropbox"
cask "google-chrome"
cask "alfred"
brew "pick"
brew "fzf"
EOF

fancy_echo "Cleaning up old Homebrew formulae ..."
brew cleanup
brew cask cleanup

if [ -r "$HOME/.rcrc" ]; then
  fancy_echo "Updating dotfiles ..."
  rcup
fi
